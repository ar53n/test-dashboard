/**
 * Media queries, общие для CSS и JS (`useMediaQuery`), чтобы раскладка и логика рендера
 * переключались на одной и той же ширине.
 */
export const media = {
  /** Дерево и таблица рядом. */
  split: '(min-width: 1280px)',
  narrow: '(max-width: 640px)',
  /**
   * Страница занимает ровно экран, панели прокручиваются внутри. На низком экране
   * (телефон в ландшафте) места под панель не остаётся — там прокручивается страница.
   */
  fitScreen: '(min-width: 1280px), (min-height: 560px)',
  /** Обратное к `fitScreen`: прокручивается вся страница. */
  scrollPage: '(max-width: 1279.98px) and (max-height: 559.98px)',
} as const
